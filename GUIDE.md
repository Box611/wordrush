# 🐣 小白使用指南

这份文档假设你**完全不懂编程**。全部照着做就行，每一步都写清楚了「打开哪个文件、改哪一行、改成什么」。

遇到任何不懂的地方，先跳到最后一节 [出错了怎么办](#出错了怎么办)。

---

## 目录

1. [三分钟上手](#1-三分钟上手)
2. [怎么改练习内容](#2-怎么改练习内容)
3. [怎么改界面上的文字](#3-怎么改界面上的文字)
4. [怎么改配色和外观](#4-怎么改配色和外观)
5. [怎么关掉不想用的功能](#5-怎么关掉不想用的功能)
6. [怎么把网站发到网上](#6-怎么把网站发到网上)
7. [怎么备份数据、换电脑](#7-怎么备份数据换电脑)
8. [怎么分享给别人](#8-怎么分享给别人)
9. [出错了怎么办](#9-出错了怎么办)
10. [改完之后的检查清单](#10-改完之后的检查清单)
11. [附：怎么重新生成词库（进阶·可选）](#附怎么重新生成词库进阶可选)
12. [附：不需要懂代码也能做的六件事](#附不需要懂代码也能做的六件事)

---

## 1. 三分钟上手

### 你现在有哪些文件

打开 `wordrush` 文件夹，会看到：

```
wordrush/
├── index.html      ← 双击这个就能打开网站
├── README.md       ← 项目说明书
├── GUIDE.md        ← 你正在读的这份
├── LICENSE         ← 开源协议
└── assets/         ← 所有素材都在这里面
    ├── css/        ← 样式（颜色、排版）
    └── js/         ← 程序（功能）和练习内容
        └── data/   ← ⭐ 练习内容在这里
```

### 开始练习

**双击 `index.html`**。浏览器会打开网站，第一轮练习已经自动开始了。

### 页面上都是什么

从上到下分五块：

| 区域 | 里面有什么 | 说明 |
|---|---|---|
| **顶栏** | 标志「单词冲刺 WordRush」 + 五个按钮 | 成绩记录 📊 / 成就徽章 🏆 / 切换主题 / 中英切换 / 设置 ⚙️ |
| **模式设置条** | 练习模式（3 选 1）、打错处理（3 选 1） | 下面一行小字会解释当前选中的组合是干什么的 |
| **内容源选择条** | 四级大纲词汇 / 六级大纲词汇 / 精选词库 / 短句 / 文章 / 网络内容源，以及「每组数量」 | 点一下立刻换内容 |
| **练习区** | 左上是内容源名称和「第几题 / 共几题」，右上是发音按钮、跳过、重新开始；中间是提示区和文字区 | **点文字区就能开始打字** |
| **底部统计栏** | 速度 WPM / 正确率 / 连击 / 得分 / 用时 / 进度条 | 每一项都能在设置里关掉 |

看到的界面大致是这样（示意，不是像素级还原）：

```
  [标志] 单词冲刺 WordRush            📊  🏆  ☀️  EN  ⚙️

  练习模式  [看英文打英文] [看中文打英文] [听发音打英文]
  打错处理  [严格模式] [宽松模式] [整句模式]

  [四级大纲词汇] [六级大纲词汇] [四级核心词] ...     每组数量 [20]

  四级大纲词汇  第 1 题 / 共 20 题              🔊  跳过  重新开始
  ────────────────────────────────────────────────────────
   中文   /əˈbændən/  vt.丢弃；放弃，抛弃
  ────────────────────────────────────────────────────────
   abandon                                    ← 点这里开始打字
  ────────────────────────────────────────────────────────

    45      98.5%     32     1280    1:24    ▓▓▓▓░░░ 15%
   速度     正确率    连击    得分    用时      进度
```

### 三条最重要的操作

| 想做什么 | 怎么做 |
|---|---|
| **开始打字** | 用鼠标点一下中间那个大方框（文字区域），然后直接敲键盘 |
| **换内容 / 换模式** | 点上面的按钮，立刻生效 |
| **重来一轮** | 按 <kbd>Tab</kbd> 键，或者点右上角「重新开始」 |

---

## 2. 怎么改练习内容

练习内容全部放在 `assets/js/data/` 这个文件夹里：

| 文件 | 里面是什么 | 能手改吗 |
|---|---|---|
| `cet4.js` | 四级精选词 466 个 | ✅ 可以，随便加 |
| `cet6.js` | 六级精选词 264 个 | ✅ 可以 |
| `phrases.js` | 常用短句 61 句 | ✅ 可以 |
| `passages.js` | 段落文章 10 段 | ✅ 可以 |
| `sources.js` | 网络内容源（VOA / BBC 风格选段） | ✅ 可以 |
| `wordbook-cet4.js` | **四级大纲词汇 4540 词**（自动生成） | ⚠️ **别手改**，会被覆盖 |
| `wordbook-cet6.js` | **六级大纲词汇 2220 词**（自动生成） | ⚠️ **别手改** |

> **为什么有两本「四级」？**
> · `wordbook-cet4.js` 是**官方大纲全量**（4540 词，带音标），适合系统性地背。
> · `cet4.js` 是**手写的 466 词精选**，文件小、格式简单，主要是给你当"照着加内容"的样板。
>
> 两个都在页面顶部有按钮，点一下就能切换，互不影响。

> **自动生成的文件别手改** —— 下次跑生成脚本会被整个覆盖。
> 想改全量词库的内容或换成别的数据源，看本文最后 [附：怎么重新生成词库](#附怎么重新生成词库进阶可选)。

> 📝 **怎么编辑这些文件？**
> 用**记事本**就能改（右键文件 → 打开方式 → 记事本）。
> 但更推荐装一个免费的 **Visual Studio Code**（[官网](https://code.visualstudio.com/)），它能给代码上色、自动提示错误，改起来轻松很多。

### 2.1 加一个单词

**打开** `assets/js/data/cet4.js`。

**往下找**，会看到一大片长这样的内容：

```js
  items: [
    ['abandon', 'v. 放弃；抛弃'],
    ['ability', 'n. 能力；才能'],
    ['absorb', 'v. 吸收；吸引'],
    ...
  ]
```

**每一行的格式**就是：

```js
    ['英文单词', '中文意思'],
```

**要加单词，就在任意一行的下面插入一行**。比如想加 `brave`：

```js
    ['ability', 'n. 能力；才能'],
    ['brave', 'a. 勇敢的'],          ← 新加的这行
    ['absorb', 'v. 吸收；吸引'],
```

### ⚠️ 三个必须遵守的规则

新手改坏文件，99% 都是踩了下面三个坑之一：

**规则 1：每行末尾要有英文逗号 `,`**

```js
    ['brave', 'a. 勇敢的'],     ✅ 正确，最后有逗号
    ['brave', 'a. 勇敢的']      ❌ 错误，会连累下一行一起报错
```

**规则 2：中英文都要用「英文双引号或单引号」包起来**

```js
    ['brave', 'a. 勇敢的'],     ✅ 用的是英文的 ' 
    ['brave', 'a. 勇敢的’],     ❌ 用的是中文的 ’
    [“brave”, “a. 勇敢的”],     ❌ 用的是中文的 “ ”
```

> 💡 一句话记住：**除了中文内容本身，其它所有符号都必须是英文半角的**（在英文输入法下敲出来的）。

**规则 3：逗号、引号、括号都是成对的，不要删掉任何一个**

建议加完以后，用 Notepad++ 或 VS Code 打开，看颜色有没有变乱 —— 变乱了说明符号少了。

### 2.2 加一个短句

**打开** `assets/js/data/phrases.js`，格式和加单词**完全一样**：

```js
    ['How are you doing today?', '你今天过得怎么样？'],
    ['I love learning English.', '我喜欢学英语。'],    ← 新加的
```

> ⚠️ 句子里的**逗号、句号、问号、撇号（don't 里的那个 '）都算一个字符**，打的时候要一模一样。
> 这本来就是短句模块想训练的东西，所以别删标点。

### 2.3 加一段文章

**打开** `assets/js/data/passages.js`。格式一样，只是内容长了：

```js
    [
      'English text goes here. It can be several sentences long.',
      '中文翻译写在这里。可以有好几句。'
    ],
```

> ⚠️ **注意**：段落模式下一段话很长，用「看中文打英文」会非常非常难。建议段落用「看英文打英文」模式。
> 如果你不希望它出现在中文模式里，把中文那部分写成 `''`（两个单引号，中间什么都不放）即可。

### 2.4 加一篇自己的文章（会出现在「一键加载」里）

**打开** `assets/js/data/sources.js`，找到这个结构：

```js
  ET.SOURCES.push({
    id: 'voa',
    group: 'web',
    unit: 'sentence',
    name: { zh: 'VOA 慢速英语风格', en: 'VOA-style Slow English' },
    desc: { zh: '短句、慢节奏，适合精听与拼写', en: 'Short sentences, slow pace' },
    texts: [
      {
        title: { zh: '睡眠与学习', en: 'Sleep and Learning' },
        body: 'Scientists say sleep is important for learning. ...'
      },
      ...
    ]
  });
```

想加一篇，就在 `texts: [` 后面照着格式加一段：

```js
      {
        title: { zh: '我的文章', en: 'My Article' },
        body: 'Write your English text here. It can have many sentences.'
      },
```

**要点**：

- `body` 里的英文可以很长，**用一整行写完**（不要按回车换行）
- 中英文之间用空格或标点隔开就行
- 系统会自动把 `body` 拆成一句一句作为题目

### 2.5 想换一个内容源的名字？

同样是 `name: { zh: '中文名', en: 'English name' }`，改引号里的字就行。

改完刷新网页，内容源按钮上就会出现新名字。

### 2.6 临时用一篇文章（不改文件）

如果不值得为一次练习去改代码：

1. 点页面上的 **「+ 自定义文本」** 按钮
2. 把英文粘贴进上面的框
3. （可选）在「中文释义」框里按行填中文，一行对应一条
4. 选「拆分方式」（按句子 / 按单词 / 按段落）
5. 点「开始练习」

**内容会自动保存在本机**，下次点顶部出现的「我的内容」按钮就能一键调出。

---

## 3. 怎么改界面上的文字

**打开** `assets/js/i18n.js`。

你会看到两大块：`zh: { ... }` 是中文，`en: { ... }` 是英文。

```js
    zh: {
      'app.name': '单词冲刺',
      'btn.skip': '跳过',
      'stat.acc': '正确率',
      ...
    },

    en: {
      'app.name': 'WordRush',
      'btn.skip': 'Skip',
      'stat.acc': 'Accuracy',
      ...
    }
```

**只要改冒号后面的内容**，别动前面的 `'app.name'` 这种键名。

比如想把「跳过」改成「换一题」：

```js
      'btn.skip': '换一题',      ✅ 可以
```

比如想改网站在浏览器标签页上的标题：

```js
      'app.docTitle': '我的英语练习站',
```

> ⚠️ **注意**：
> 1. 中英文两块里的键名（冒号左边）必须一模一样，改文字只改右边
> 2. 想改中文界面就改 `zh` 那块，改英文界面就改 `en` 那块
> 3. 改完记得保存文件，然后刷新网页

### 找不到某句话对应的键名怎么办？

在 `i18n.js` 里搜索那句话本身。比如不知道「正确率」是从哪来的，搜「正确率」，就能看到 `'stat.acc': '正确率'`。

---

## 4. 怎么改配色和外观

**打开** `assets/css/style.css`，文件**最上面**就是配色区。

### 改主题色（比如把紫色改成蓝色）

找到这一段（大约在第 100 行左右）：

```css
html[data-theme="light"] {
  --bg:            #f5f6fa;   /* 页面底色 */
  --surface:       #ffffff;   /* 卡片底色 */
  --text:          #1b2030;   /* 主要文字 */
  --primary:       #4f46e5;   /* 主色 ← 改这个 */
  ...
}
```

把 `--primary` 后面的色号换掉就行。常用色号：

| 颜色 | 色号 |
|---|---|
| 靛蓝（原色） | `#4f46e5` |
| 天空蓝 | `#0ea5e9` |
| 青色 | `#0d9488` |
| 橙色 | `#f97316` |
| 玫红 | `#e11d48` |
| 紫罗兰 | `#7c3aed` |
| 墨绿 | `#059669` |

> 💡 想要更多颜色，去搜索「颜色选择器 在线」（比如 <https://www.w3school.com.cn/colors/colors_picker.asp>），选一个喜欢的，把 `#` 开头的 6 位色号复制过来。

### 三套主题各自独立

文件里有三块，分别对应浅色、深色、学习风：

```css
html[data-theme="light"] { ... }   /* 浅色 */
html[data-theme="dark"]  { ... }   /* 深色 */
html[data-theme="study"] { ... }   /* 学习风 */
```

**想让三套主题都换主色，就得三块都改一遍。** 只想改其中一套，就只改那一块。

### 改字大小 / 圆角

同样在文件最上面的 `:root { ... }` 里：

```css
  --r-md: 14px;    /* 中等圆角，数字越大越圆 */
  --sp-4: 16px;    /* 中等间距，数字越大越松散 */
```

及练习文字的大小：

```css
  --type-size: clamp(1.3rem, 1.05rem + 1.15vw, 2rem);
              /* └最小值    └随屏幕缩放      └最大值 */
```

把三个数字一起按比例调大或调小即可。

> 🎁 **更简单的办法**：页面「设置」里已经内置了**四档字号**（小 / 中 / 大 / 特大），日常使用不用改代码。

---

## 5. 怎么关掉不想用的功能

**最简单的方式：完全不用改代码。**

点右上角 ⚙️ **设置**，里面**每一项都是独立的开关**：

| 设置分组 | 可以关掉什么 |
|---|---|
| **外观** | 换主题、换界面语言、调字号 |
| **练习** | 关掉「自动下一题」、关掉「打乱顺序」（改成按顺序连续练）、关掉「显示音标」 |
| **发音** | 换发音来源、换口音、**看英文模式自动发音**、听音模式自动播放、答对后朗读、调语速 |
| **趣味与激励** | 关掉速度 / 正确率 / 连击 / 得分 / 用时 / 进度条 / 打字音效 / 每日目标 / 成就徽章 / 连续打卡 |
| **数据** | 导出备份、导入备份、**重置练习进度**、恢复默认设置、清空所有数据 |

关掉之后界面会立刻变干净 —— 统计栏里被关掉的项目会直接隐藏，不是变灰。

> 全部设置都会自动记住，下次打开还是你调好的样子。

### 5.1 「打乱顺序」关掉之后会怎样（长词库必看）

四级大纲有 4540 个词。如果一直随机抽 20 个，等于永远练不到后面。

**关掉「打乱顺序」后：**

1. 变成**按字母顺序连续练**（abandon → ability → able → …）
2. 练习区右上角会显示 **「第 1–20 / 4540 条」**，一眼看到练到哪了
3. 每开始新一轮**自动接着上次的位置**，不用手动记
4. 练到第 4540 条后**自动绕回开头**，循环下去
5. 想从头再来：**设置 → 数据 → 重置练习进度**

> 进度只记在本机。导出备份时会一起带走，换电脑也不会丢。

### 5.2 音标怎么关

「看英文打英文」模式下会在提示区显示音标（像 `/əˈbændən/`）。不想看就：
**设置 → 练习 → 显示音标 → 点一下关掉**。

> 注意：音标**只在「看英文打英文」模式显示**。
> 因为音标差不多等于把拼写念出来了，在「看中文打英文」和「听音打英文」里显示等于直接给答案。

### 想改「默认值」（新用户第一次打开时的样子）

**打开** `assets/js/config.js`，找到 `ET.DEFAULT_SETTINGS = { ... }`：

```js
  ET.DEFAULT_SETTINGS = {
    theme: 'light',          // 默认主题：light 浅色 / dark 深色 / study 学习风
    lang: 'zh',              // 默认语言：zh 中文 / en 英文
    fontSize: 'md',          // 默认字号：sm / md / lg / xl

    practiceMode: 'en2en',   // 默认练习模式
    errorMode: 'strict',     // 默认打错处理
    autoNext: true,          // 是否自动下一题
    itemCount: 20,           // 每组题目数量 ← 想改成 10 个就写 10

    sound: true,             // 打字音效
    ...                      // 每行后面都有中文注释
  };
```

> ⚠️ 改这里只影响**没设置过的用户**。你自己浏览器里已经保存的设置优先级更高 —— 想让改动生效，去设置面板点「恢复默认设置」，或者清空浏览器数据。

---

## 6. 怎么把网站发到网上

发到网上之后，**手机、平板、别人的电脑都能打开**，还能把链接发给同学。

你有两条路可选，**都不会写代码就用路线 A**。

### 路线 A：网页上点鼠标（不用装任何软件）

#### 第 1 步：注册 GitHub

打开 <https://github.com> → 点 **Sign up** → 按提示注册（免费）。把用户名记下来。

#### 第 2 步：创建仓库

1. 登录后，点页面右上角的 **`+`** → **New repository**
2. **Repository name** 填：`wordrush`
3. 选 **Public**
4. ⚠️ **不要**勾选 "Add a README file"
5. 点绿色的 **Create repository**

#### 第 3 步：上传文件

1. 在新页面点 **uploading an existing file** 这个蓝色链接
2. 打开你电脑上的 `wordrush` 文件夹
3. **全选里面的所有内容**（<kbd>Ctrl</kbd> + <kbd>A</kbd>），**拖进**网页中间那个框里
   - ⚠️ **重点**：要拖**文件夹里面的东西**（`index.html`、`assets` 文件夹等），
     不要把 `wordrush` 这个文件夹本身整个拖进去 —— 否则会多套一层目录，网站打不开
4. 等文件全部上传完（看到一排文件名）
5. 往下滚，点绿色的 **Commit changes**

#### 第 4 步：开启网站

1. 上传完后，点仓库上方的 **Settings**（设置）
2. 左侧菜单往下找，点 **Pages**
3. **Source** 选 `Deploy from a branch`
4. **Branch** 选 `main`，右边的文件夹选 `/ (root)`
5. 点 **Save**
6. 等 1 ~ 2 分钟，**刷新这个页面**，顶部会出现一行绿色背景的网址：

```
https://你的用户名.github.io/wordrush/
```

**这个网址就是你的网站！** 发给任何人、在任何设备上都能打开。

#### 以后想更新内容

改完自己电脑上的文件后，回到仓库页面 → 找到对应的文件 → 点铅笔图标编辑 / 或点 **Add file → Upload files** 重新上传 → Commit。等 1 分钟自动生效。

---

### 路线 B：用命令行（稍微熟悉电脑操作的话，更快）

先在电脑上装 **Git**（[下载](https://git-scm.com/downloads)），然后在 `wordrush` 文件夹里**空白处右键 → Open Git Bash here**，依次执行下面 5 条命令：

```bash
git init
git add .
git commit -m "第一次提交"
git branch -M main
git remote add origin https://github.com/你的用户名/wordrush.git
git push -u origin main
```

> 把 `你的用户名` 换成你真实的 GitHub 用户名。
> 第一次 `push` 会弹出登录窗口，按提示登录 GitHub 即可。

推送完成后，同样去 **Settings → Pages** 开启（就是路线 A 的第 4 步）。

以后每次改完内容，只要三条命令：

```bash
git add .
git commit -m "更新内容"
git push
```

---

### 路线 C：部署到 Vercel / Netlify（比 GitHub Pages 快）

不想用 GitHub 也行。打开 <https://app.netlify.com/drop>，**把整个 `wordrush` 文件夹拖进去**，几秒钟就给你一个网址。注册一下账号就能长期保留。

⚠️ **关键提示**：这三个平台都会问你「构建命令（Build command）」是什么，**全都留空**。
因为本项目是纯静态网站，不需要任何构建 —— 填了反而会报错。

---

## 7. 怎么备份数据、换电脑

你的练习记录（成绩、打卡、徽章）只存在**你自己这台电脑的这个浏览器**里。

> 👉 换句话说：**用另一台电脑打开同一个网址，看不到你之前的记录。** 这是设计如此 —— 网站没有服务器，你的数据不上传。

### 备份

1. 点右上角 **📊 图标**（成绩记录）
2. 点 **导出备份 (JSON)**
3. 浏览器会下载一个文件，叫 `wordrush-backup-20260918-1044.json`
4. 把它存到网盘 / U 盘 / 微信收藏，随便哪都行

### 恢复 / 换电脑（导入备份）

**操作入口有两个，效果完全一样，任选一个：**

- 点右上角 **📊 图标**（成绩记录）→ 弹窗底部点 **「导入备份」**
- 或点右上角 **⚙️ 图标**（设置）→ 拉到底部「数据」→ 点 **「导入备份」**

**完整流程（四步）：**

1. 点「导入备份」→ 弹出系统的**文件选择框**
2. 找到并选中之前存的那个 `.json` 文件（名字形如 `wordrush-backup-20260918-1044.json`）
3. 页面会弹出一个**确认框**，并排显示两份信息让你核对：

```
┌────────────────────────────────────────────┐
│ 确认导入备份                          ✕    │
├────────────────────────────────────────────┤
│ 备份导出时间：2026/9/18 11:20（v1.0.0）     │
│                                            │
│ 这个备份文件里有                            │
│  ┌──────┐ ┌──────┐ ┌─────┐ ┌─────────┐   │
│  │  32  │ │  12  │ │  5  │ │    2    │   │
│  │成绩记录│ │打卡天数│ │徽章 │ │自定义文本│   │
│  └──────┘ └──────┘ └─────┘ └─────────┘   │
│                                            │
│ 本机当前：成绩 8 条、打卡 3 天、徽章 1 个… │
│                                            │
│ ⚠️ 导入会把本机数据替换成备份文件里的内容， │
│    且无法撤销。建议先点「导出备份」……      │
├────────────────────────────────────────────┤
│                    [取消]  [确认导入]       │
└────────────────────────────────────────────┘
```

4. 核对无误 → 点 **「确认导入」**；不想导入了 → 点 **「取消」**，**数据不会有任何变化**

> 💡 **重点：在你点下「确认导入」之前，网站不会改动任何数据。**
> 所以就算点错了文件、弹出确认框后又反悔，直接取消就好，不会有任何损失。

### 选错文件了会怎样？

网站会告诉你**具体**哪里不对，而不是笼统地说「格式错误」：

| 你看到的提示 | 说明 | 怎么办 |
|---|---|---|
| 这个文件是空的 | 选到了一个空文件 | 重新选一个 |
| 这个文件不是有效的 JSON | 选错了文件（比如选成了网页、Word 文档、txt） | 重新选「导出备份」生成的那个 `.json` |
| 这个 JSON 不是本项目的备份文件 | 文件是 JSON，但不是这个网站的备份（缺项目标识） | 确认文件来源；或看下面「手动写一个备份」 |
| 文件读取失败 | 文件损坏了 | 换一份备份，或重试一次 |

### 手动写一个备份（进阶，可选）

备份文件就是普通文本，**用记事本能打开，也能自己写一个**。最简的可用格式：

```json
{
  "app": "wordrush",
  "records": []
}
```

> ⚠️ `"app": "wordrush"` 这一行**必须保留**，它就是「项目标识」，
> 删掉或写错，导入时会被拒绝（这是为了防止你误导入别的网站的文件）。
>
> 💡 早期版本的标识是 `english-typing-practice`，写这个**也能正常导入** ——
> 所以如果你手上有更早导出的备份，不用改，直接导入就行。

**几个实用玩法：**

| 想做的事 | 怎么写 |
|---|---|
| 清空成绩，但保留打卡和徽章 | `"records": []` |
| 只导入自定义文本，其他都不动 | 只写 `app` 和 `customTexts`，不写 `records` / `daily` / `badges` |
| 把打卡天数改成指定几天 | 写 `"daily": { "2026-09-18": { "chars": 1, "seconds": 60, "items": 1, "sessions": 1 } }` |

**规则：文件里没写到的部分，本机数据保持不动。** 所以你可以只替换某一类数据，其余不受影响。

> ⚠️ 成绩记录里的 `ts`（时间戳）必须是数字且合法，格式不对的记录会在导入时被自动丢掉。
> 如果你只是想把重复记录删掉，直接编辑 `records` 数组即可，不用管时间戳。

### 多久备份一次

- 只是随便练练 → 一个月一次足够
- 连续打卡中、很在意记录 → 每周一次
- 打算清理浏览器数据 / 换电脑 → **一定先备份**

---

## 8. 怎么分享给别人

**注意：如果你按第 6 节把网站发到了网上，那「分享」就只是把网址发出去。**

对方打开网址就能用，而且**你们的数据互相独立**（各自的记录存在各自的浏览器里），不会串。

如果想连内容一起分享（比如你自己加的词库）：

1. 把整个 `wordrush` 文件夹打包成 zip 发给对方
2. 对方解压后双击 `index.html` 就能用

---

## 9. 出错了怎么办

### 第一步：打开控制台看报错

按 <kbd>F12</kbd>（笔记本可能是 <kbd>Fn</kbd> + <kbd>F12</kbd>），切到 **Console** 标签。

- **红色文字**就是报错。看它提到是哪个文件、第几行。
- 通常长这样：`cet4.js:245 Uncaught SyntaxError: Unexpected token` ——
  意思是 **`cet4.js` 第 245 行有语法错误**，多半是少了逗号或引号。

### 第二步：对照下面这张表

| 现象 | 最可能的原因 | 怎么修 |
|---|---|---|
| 打开一片空白 | 某个 js 文件被改坏 | 按 F12 看 Console，报错指到的那个文件就是元凶 |
| 页面正常，但某几个词不见了 | 那几行的格式写错 | 检查有没有少逗号 / 用了中文引号 |
| 报错说 `Unexpected token` | 少逗号、多逗号、引号不配对 | 看报错的行号，和上下行对比一下格式 |
| 报错说 `Unexpected end of input` | 括号或引号少了一个 | 检查文件末尾，`]`、`}`、`;` 有没有少 |
| 改完没变化 | 浏览器缓存 | 按 <kbd>Ctrl</kbd> + <kbd>F5</kbd> 强制刷新 |
| 发音没声音（特别是第一题） | 浏览器自动播放限制：页面刚打开、你还没点过任何东西时，浏览器不允许自动出声 | 先点一下页面任意位置再听；本项目会在你第一次点击时自动补播一次。手动点 🔊 按钮永远有效 |
| 发音特别慢（等十几秒） | 发音来源选了「Free Dictionary 真人录音」 | 改成 **设置 → 发音 → 发音来源 → 自动（推荐）**。那个来源音质最好但实测要 20 秒 |
| 看中文模式没有发音 | 这是**故意的** | 该模式的英文就是答案，读出来等于直接告诉你答案。想看发音请切到「看英文打英文」模式 |
| 看中文模式没有音标 | 同样是**故意的** | 音标基本等于把拼写念出来。音标只在「看英文打英文」模式显示 |
| 看英文模式不自动发音 | 开关被关掉了 | 设置 → 发音 → 打开「看英文模式自动发音」 |
| 顺序练习的进度丢了 | 点过「重置练习进度」/ 清了浏览器数据 / 用无痕模式 | 进度只在本机，正常；重新开始即可。以后可和成绩一起用备份带走 |
| 顺序练习倒数第二轮不按顺序 | 检查「打乱顺序」是否被打开了 | 打乱模式下不使用进度，关掉即可 |
| 数据不见了 | 清理过浏览器数据 / 用了无痕模式 | 用备份导入恢复；以后记得定期备份 |
| 导入备份没反应 | 文件选错了类型，或文件不合法 | 看「选错文件了会怎样」那张表，提示会说明具体原因 |
| 导入后数据没变 | 备份文件里没写对应字段 | 正常行为：文件里没带到的部分不覆盖。先导出一次看看完整格式 |
| 导入后成绩少了几条 | 那几条记录的时间戳格式不合法，被自动过滤了 | 用记事本打开备份，检查 `records` 里每条是否都有数字型的 `ts` |
| 手机上打开错位 | 系统浏览器太老 | 换 Chrome 或 Edge 试试 |

### 第三步：最省事的恢复方法

如果你把文件改坏了又不知道错在哪：

1. 从网上下载一份**干净的原始项目**（或者重新解压你最初下载的压缩包）
2. 把你**只改过的那几个文件**（比如只有 `cet4.js`）用干净的版本覆盖回去
3. 然后对着干净版本，慢慢把你想加的内容重新加一遍 ——
   每次加完刷新一次页面，就能立刻知道是不是刚加的那一行有问题

> 💡 **一个好习惯**：改文件之前先复制一份，改名成 `cet4.js.bak` 放在旁边。改坏了一键换回来。

### 真的搞不定

把下面这些信息发给懂技术的朋友，对方基本能立刻定位问题：

- 你改了哪个文件
- 改之前长什么样、改之后长什么样
- <kbd>F12</kbd> → Console 里的红色报错（截图即可）

---

## 10. 改完之后的检查清单

每次改完文件，按这个顺序过一遍，能省掉大部分麻烦：

- [ ] **保存文件**了吗（<kbd>Ctrl</kbd> + <kbd>S</kbd>）
- [ ] 刷新网页（<kbd>Ctrl</kbd> + <kbd>F5</kbd>，强制刷新）
- [ ] 按 <kbd>F12</kbd> 看 Console 有没有红色报错
- [ ] 点开对应的内容源按钮，确认新内容出现了
- [ ] 如果加了内容，点「重新开始」看看能不能正常出题
- [ ] 如果加了中文释义，切到「看中文打英文」验证一下中文有没有正确显示
- [ ] 改完满意了，记得**再备份一次数据**，并把文件上传到 GitHub 更新线上版本

---

## 附：怎么重新生成词库（进阶·可选）

两本大纲词书（`wordbook-cet4.js`、`wordbook-cet6.js`）是**自动生成**的。
只有想「更新词库」或「换成自己的数据源」时才需要做这一步，**平时完全不用管**。

### 什么时候需要它

- 想换成自己的词表（比如你有一份老师给的词单）
- 想换成别的出处（比如觉得当前数据源的释义不够好）
- 想更新到更新版本的词库

### 需要什么

一台装了 **Node.js** 的电脑（[下载地址](https://nodejs.org/)）。
⚠️ **注意：只有这一步需要 Node.js，网站本身不需要。**

### 步骤

1. 打开 `tools/build-wordbook.mjs`
2. 想换数据源，就改文件最上面的这两处：

```js
const CDN = 'https://cdn.jsdelivr.net/gh/mahavivo/english-wordlists@master/';

const BOOKS = [
  {
    key: 'cet4full',
    file: 'CET4_edited.txt',    // ← 改成你的文件名
    ...
```

3. 在项目根目录（`wordrush` 文件夹）打开命令行，执行：

```bash
node tools/build-wordbook.mjs
```

4. 看到这样的输出就成功了：

```
[cet4full] 四级大纲词汇
  · 解析行数 4696
  · 跳过：空行 53 / 标题与分节 28 / 格式不符 0 / 释义无中文 0
  · 同词不同词性合并 75 次
  · 得到词条 4540（带音标 4530）
  · 输出 assets\js\data\wordbook-cet4.js（253 KB）
```

5. **刷新网页**，新词库就生效了。

### 关于原始数据文件

- 脚本会把下载到的原始文件缓存在 `tools/raw/` 里，**下次离线也能重新生成**。
- 这个目录已经写进 `.gitignore`，不会被提交到 GitHub（属于缓存，删了也没事）。
- 想离线跑，可以自己把原始 `.txt` 放进 `tools/raw/`，脚本会优先用本地文件。

### ⚠️ 版权提醒（重要）

当前数据源 `mahavivo/english-wordlists` **没有声明开源协议**。
词条来自公开的四六级考试大纲词汇表，中文释义由该仓库整理。

**如果你打算把这个网站公开分享或发布到 GitHub：**
- 项目里的数据文件头部和按钮悬停提示都已经标注了出处
- 但你可能仍需要自行评估是否需要取得许可
- 更稳妥的做法：换成自己整理的词表，或使用明确允许再分发的数据源

**不想要这两本词库？** 参考 [README 的常见问题](README.md#常见问题)，
删掉两个文件 + `index.html` 里两行 `<script>` 即可，网站会自动退回精选词库。

---

## 附：不需要懂代码也能做的六件事

1. **改默认每组题目数量**：`assets/js/config.js` 里搜 `itemCount`，把 `20` 改成你想要的数字
2. **改默认主题为学习风**：`assets/js/config.js` 里搜 `theme:`，把 `'light'` 改成 `'study'`
3. **关掉看英文模式的自动发音**：`assets/js/config.js` 里搜 `autoSpeakEn2en`，把 `true` 改成 `false`
   （或者直接在页面「设置 → 发音」里关，更简单）
4. **删掉一个内容源**：`assets/js/data/sources.js` 里，把整个 `ET.SOURCES.push({ ... });` 代码块删掉即可
5. **删掉两本全量词库**（体积大 / 版权顾虑）：删掉 `assets/js/data/wordbook-cet4.js` 和
   `wordbook-cet6.js`，再移除 `index.html` 里对应的两行 `<script>` 标签。网站会自动退回精选词库
6. **改默认发音源**：`assets/js/config.js` 里搜 `voiceProvider`，
   把 `'auto'` 改成 `'youdao'` / `'baidu'` / `'dictionaryapi'` / `'browser'`

---

祝练习顺利。有问题先看第 9 节，90% 的坑都在那张表里 🙂
